KAIROS CI + UI FIX

1. Extract this ZIP into the repository root and allow replacements.
2. Delete every path listed in _FILES_TO_DELETE.txt.
3. Commit file deletions as well as additions/modifications.
4. Run: bash ./scripts/verify_kairos.sh
5. Push and let GitHub Actions run the full Gradle test/lint/build workflow.

The CI verifier is deliberately invoked with bash, so it does not depend on
scripts/verify_kairos.sh retaining its executable bit after ZIP extraction or Git checkout.
